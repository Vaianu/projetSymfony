<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenue aux Enchères{% endblock %}</title>
        {% block stylesheets %}<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">{% endblock %}
    </head>
    <body>
        {% block body %}
		<h1>Bienvenue aux Enchères</h1>
		<div>
		{% if app.user %}
			You are logged in as {{ app.user.username }} <a href="{{ path('app_logout') }}" class="btn btn-outline-primary" role="button">Logout</a>
			{#<p>Vous avez {{ nbjetons }} jetons gratuit offert par la maison</p>#}
			<p><a href="{{ path('utilisateur_achat') }}">Acheter des jetons</a></p>
		{% else %}
			<a href="{{ path('app_login') }}" class="btn btn-outline-primary" role="button">SE CONNECTER</a>
			<a href="{{ path('app_register') }}" class="btn btn-outline-primary" role="button">S'INSCRIRE</a>
		{% endif %}
		</div>
		{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
